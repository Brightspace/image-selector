{
  "name": "d2l-image-selector",
  "description": "Polymer-based web component for a D2L image selector",
  "private": true,
  "scripts": {
    "postinstall": "polymer install --variants",
    "lint": "npm run lint:html && npm run lint:wc",
    "lint:html": "eslint *.html demo/*.html test/**/*.html test/**/*.js",
    "lint:wc": "polymer lint -i *.html",
    "test:polymer:local": "polymer test --skip-plugin sauce",
    "test:polymer:sauce": "polymer test --skip-plugin local",
    "test": "npm run lint && npm run test:polymer:local"
  },
  "author": "D2L Corporation",
  "license": "Apache-2.0",
  "devDependencies": {
    "eslint": "^5.9.0",
    "eslint-config-brightspace": "^0.4.1",
    "eslint-plugin-html": "^5.0.0",
    "polymer-cli": "^1.9.1"
  }
}
